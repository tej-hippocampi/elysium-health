# Secrets — never commit
backend/.env
**/.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# macOS
.DS_Store

# Auth (user store — do not commit)
backend/auth_users.json

# Landing (dependencies and build)
landing/node_modules/
landing/dist/
